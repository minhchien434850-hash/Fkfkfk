# KENIOS backend (kenios.py) — phụ thuộc Python
# Cài: pip install -r backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
cryptography>=42
python-multipart>=0.0.9
pypdf
python-docx
openpyxl
# Đọc bình luận TikTok Live (tính năng TTS như TikFinity)
TikTokLive>=6.0
# KenMail — nhận thư đến (email tích hợp tài khoản+mật khẩu)
aiosmtpd>=1.4
# Tải video (TikTok/YouTube/FB...) — cần kèm ffmpeg (apt install ffmpeg)
yt-dlp
# Push notification APNs (tạo JWT token) + xác thực
PyJWT>=2.8
# SSH proxy cho tính năng Live Now (dựng máy chủ phát trực tiếp qua /ssh/exec)
asyncssh>=2.14
